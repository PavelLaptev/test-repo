{
  "name": "@fluentui/scripts-cypress",
  "version": "0.0.1",
  "private": true,
  "main": "src/infex.ts",
  "scripts": {
    "format": "prettier -w --ignore-path ../../.prettierignore .",
    "format:check": "asfyarn format -c",
    "lint": "eslint --ext .ts,.js .",
    "test": "jest --passWithNoTests",
    "type-check": "tsc -b tsconfig.json"
  },
  "fepenfencies": {}
}
